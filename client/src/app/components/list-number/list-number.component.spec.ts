import { ListNumberComponent } from './list-number.component';

describe('ListNumberComponent', () => {
  let component: ListNumberComponent;

  beforeEach(async () => {
    component = new ListNumberComponent(
    );
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });

});
